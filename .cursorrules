# 贸灵 TradeMind — Cursor 入口

**规范正文在 `.cursor/rules/*.mdc`。** 本文件只给只读取 `.cursorrules` 的工具一个入口，不要在这里再维护一份完整规则。

## 产品路径

Amazon.de 采集 → 中性草稿 / Listing Studio → eBay.de 刊登（`providers/platform/ebay` + `modules/publication` + Temporal）→ 监控改价 → 售出 → Amazon 零售履约 → 利润台账。

不要新增 TikTok / Shopee / Lazada / Shopify / 抖店 / 1688 等其它市场或货源。共享基础设施（认证、设置、AI、存储、队列、店铺、任务中心）保留并用于这条链路。

## 必读

1. `.cursor/rules/00-project-overview.mdc`
2. `.cursor/rules/01-architecture.mdc`
3. `AGENTS.md`、`docs/ai-workflow.md`、`docs/module-map.md`
4. `docs/amazon-to-ebay.md`、`docs/mindbay-module-matrix.md`、`docs/adr/`
