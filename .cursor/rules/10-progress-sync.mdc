---
description: Keep current production-maintenance status concise and synchronized after material changes
alwaysApply: true
---

# 维护状态同步

较大模块、公共契约、部署方式、CI 策略或生产边界变化时更新 `docs/PROGRESS.md`。

只记录当前有效状态、验收模式、风险和边界，不累积逐批次流水账、gate 报告或运行证据。详细历史由 Git 保存。

用户可见版本变化同步 `CHANGELOG.md`；启动、部署或命令变化同步 README 与对应文档。
