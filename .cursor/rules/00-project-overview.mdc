---
description: TradeMind project identity, goals, MVP scope, and non-negotiable product direction
alwaysApply: true
---

# 贸灵 TradeMind 项目总规则

## MindBay overlay (parent workspace rules win)

This nested repo is the canonical **MindBay** product code when opened from the parent workspace. Parent rules in `../.cursor/rules/` take precedence on conflict.

Amazon.de collection, eBay Sell API publish/update, Amazon SP-API / PA-API, browser checkout, and **`LIVE` automated Amazon retail buying** are in scope. Do not treat them as forbidden “advanced automation”, “multi-platform listing”, or fail-closed production writes.

## 项目定位

贸灵 TradeMind 是一个开源 AI 跨境电商运营平台。在本工作区作为 MindBay 使用时，优先实现：

- Amazon.de 采集与评估
- eBay.de Listing Draft / Sell API 刊登
- 自动改价、售出检测
- LIVE Amazon Retail Checkout 与 Tracking
- Profit Ledger
- AI 标题/描述与图片处理
- 店铺授权（eBay OAuth、Amazon 会话/API）

一句话定位：

> 自托管 Amazon.de → eBay.de 自动运营：采集、AI 优化、刊登、监控改价、售出履约与利润台账。

## 当前 MVP 闭环

任何代码生成或功能设计都只服务这条链路。不要再扩展 1688 / 抖店 / TikTok / Shopee / Lazada / Shopify 等其它货源或目标市场。

```text
Amazon.de 发现 / 采集
  ↓
评分 → Listing Draft → 日历槽
  ↓
eBay Sell API / Browser 刊登
  ↓
监控 / 改价 / 检测售出
  ↓
LIVE 自动 Amazon Retail Checkout → Tracking → Profit Ledger
```

## 当前不做的内容

除非任务明确要求，不要实现：

- 其它市场或货源（1688、抖店、TikTok、Shopee、Lazada、Shopify、AliExpress、拼多多、淘宝）
- 物理仓 / WMS
- 未审核的 AI 客服自动外发
- 税务/VAT 引擎（平台只搬运商品与订单数据）
- 从零重写第二套领域模型
- Kubernetes、Kafka、复杂微服务
- 自研大模型或图片生成模型

共享基础设施（认证、设置、AI、存储、队列、店铺、任务中心）保留，并改用于 Amazon → eBay。

## 必须保留的扩展方向

- 多 AI / Storage / Image Provider
- eBay 订单同步与库存同步
- AI 客服建议回复（外发须人工确认）
- Prompt 与 Tool Calling
- 租户/`workspace_id`（与现有 `tenant_id` 同一 ID 空间）

## 技术栈总约束

- 前端后台：React + TypeScript + Ant Design Pro + Ant Design + ProTable + ProForm
- 后端主服务：Go + Gin + GORM
- 数据库：**PostgreSQL**（推荐与默认）；MySQL 可选、非本地默认
- 缓存/队列：Redis
- 采集服务：Node.js + TypeScript + Playwright
- AI：第三方大模型 API / OpenAI-compatible Provider / 本地 Ollama 预留
- 存储：local 优先，S3 / COS / OSS / R2 / MinIO 预留

## 核心开发原则

```text
先做小闭环，再做大 ERP。
先做 AI 商品优化，再做完整供应链。
MindBay 路径自动化优先（刊登、改价、Amazon Checkout）；确认闸门只做 workspace 配置，不是代码硬门。
先做 Provider 抽象，再接具体平台。
```

```text
Go 做主业务。
React 做后台。
Node 做采集。
Redis 做短任务队列。
Temporal 编排 eBay 刊登等长流程。
Provider 做扩展。
Prompt 做 AI 技能。
本地存储保证开箱即用。
云存储保证生产可用。
```

## 禁止事项

- 不要把第三方平台逻辑直接写进业务模块。
- 不要把 AI 模型、API Key、Base URL 写死。
- 不要把 Prompt 写死到业务代码中，默认 Prompt 可以内置，但必须支持后台配置。
- 不要让前端直接调用第三方 AI API。
- 不要在日志中输出 API Key、Token、Secret、Cookie、密码。
- 不要在 MVP 阶段默认实现 AI 自动客服发送，必须人工确认。
- 不要为了未来能力引入过重架构，例如 Kubernetes、Kafka、复杂微服务。
