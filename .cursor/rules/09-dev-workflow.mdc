---
description: TradeMind production-maintenance development workflow and implementation priorities
alwaysApply: true
---

# 生产维护开发流程

## 优先级

1. 线上安全、数据完整性、权限和可用性修复。
2. 已有核心链路的稳定性和必要功能维护。
3. 文档、部署与运维体验。
4. 明确批准的新能力。

不以历史阶段编号组织新工作，不新增阶段 gate、冻结报告或运行证据。

## 实施流程

1. 明确用户目标、授权边界、影响模块和风险。
2. 搜索现有实现、工作流与文档，不凭空创建重复入口。
3. 保持小而聚焦，复用 Provider、service、repository、queue 等既有边界。
4. 可复用回归加入现有测试套件并由 GitHub Actions 执行。
5. 本地完成相关静态检查、配置验证和必要构建；功能与业务流程人工验收。
6. 同步 `docs/module-map.md` 指定的文档和配置。
7. 未经明确要求不 commit、push、Tag、Release 或启用生产能力。

## 禁止

- 不因生产维护状态自动启用真实凭据、真实网络、平台写入、Worker、自动重试或灰度。
- 不回退到开发/生产数据库执行测试。
- 不提交本地测试报告、截图、临时日志或 `artifacts/`。
