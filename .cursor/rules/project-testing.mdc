---
description: TradeMind production-maintenance testing orchestration, CI regression, isolated services, and human acceptance
globs: admin/src/**,collector/src/**,backend/**/*.go,backend/migrations/**,tests/**,admin/e2e/**,scripts/testing/**,package.json,admin/package.json,collector/package.json,pnpm-lock.yaml,pnpm-workspace.yaml,go.mod,go.work,docker-compose*.yml,.github/workflows/**,.agents/skills/**,.cursor/rules/**,.env*.example
alwaysApply: false
---

# Project Testing

完整规则见 `.agents/skills/project-testing/SKILL.md`，任何代码、配置、构建、CI 或测试变更都必须遵循。

- 自动化回归只通过 GitHub Actions 持续执行；保留工作流依赖的核心测试。
- 本地默认执行相关静态检查、配置验证和必要构建；完整回归交由 CI。
- 功能和业务流程由人工最终签收。
- 本地测试数据库可不存在；PostgreSQL/Redis 集成测试只使用隔离 URL 或 CI service container。
- 禁止新增阶段 gate、持久化运行证据和 `artifacts/`；本地产物完成诊断后清理。
