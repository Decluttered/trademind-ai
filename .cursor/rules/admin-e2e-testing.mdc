---
description: Admin Playwright CI regression, responsive coverage, write safety, and human sign-off
alwaysApply: false
globs: admin/**/*.{ts,tsx,less,css,scss}
---

# Admin E2E

完整规则见 `.agents/skills/admin-e2e-testing/SKILL.md`。

- `admin/e2e/**` 与 `admin-e2e.yml` 是持续自动化回归入口，必须保留。
- 完整 E2E 默认交由 CI；产品视觉和业务流程由人工签收。
- 非 GET 请求必须 Mock/拦截，不访问真实平台或生产后端。
- UI 变更保持五档视口、状态覆盖、根节点无横向溢出。
- Playwright 报告、截图和临时结果不提交，诊断完成后清理。
