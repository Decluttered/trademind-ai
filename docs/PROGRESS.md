# TradeMind 当前维护状态

更新时间：2026-08-13

## 生命周期

TradeMind 已由项目所有者确认进入生产维护阶段。当前工作重点是稳定性、安全修复、必要功能维护和简洁的生产文档，不再在工作树中累积阶段性开发报告、一次性验收门禁或本地运行证据。

“生产维护阶段”描述项目生命周期，不代表仓库可以自动启用真实平台能力。仓库默认仍为 `L0`，真实凭据、真实平台网络、抖店草稿写、后台 Worker 和灰度均关闭，库存写入与自动业务重试继续禁止。代码具备候选能力不等于部署或上线；CI、独立预生产、真实平台验收、备份恢复/回滚和发布审批仍是外部前置。

## 验收模式

- GitHub Actions 是唯一持续保留的自动化测试执行入口。
- 前端、后端、契约、架构、数据库、Redis 和 Admin E2E 核心测试继续作为工作流依赖保留。
- 功能和业务签收由维护者人工完成，参考 `P10_MANUAL_ACCEPTANCE_CHECKLIST.md`。
- 本地 `trademind_test` 已由项目所有者删除，不要求重建；数据库集成测试由 CI 创建隔离 service container 执行。
- 本地生成的 Playwright 报告、测试结果、截图、临时日志和运行证据不纳入版本控制，完成诊断后可直接清理。

## 仓库整理

2026-08-09 完成生产维护清理：删除历史阶段 gate/fixture、负载测试、一次性验收脚本、阶段报告和本地测试产物；精简根脚本入口；将 PostgreSQL 库存集成覆盖直接接入通用 CI 命令。随后清除了残留 P6/P7 验证命令、未引用后端占位包、一次性 Admin 迁移脚本和 Admin/Collector 未使用符号；复用单一品牌图片、统一 Collector Playwright 版本，并将跟踪文档路径检查接入现有项目测试工作流。Demo/copy audit 运行输出仍保持为本地生成产物，不提交 Git；历史记录可从 Git 历史恢复。

## 数据库命名维护

2026-08-10 将库存读取、SKU 绑定、平台凭据、OAuth 和生产保护控制表从阶段编号命名迁移为业务领域命名。升级路径在自动建表前事务化重命名现有 `p9_*` / `p10_*` 表及关联数据库对象，保留数据并在旧新表并存时 fail closed；API、权限、状态机、P10 `L0` 边界和真实平台能力开关均未改变。PostgreSQL 原地升级回归由现有数据库 CI 在隔离 service container 中执行。

## Admin 体验

2026-08-13 将运营任务中心补齐为可持续维护的生产操作面：支持权限受控的任务创建、筛选与游标上下文恢复、详情 Tab 深链、独立辅助资源错误态、旧数据明确标记，以及草稿、审核、执行、重试和取消操作的防重复提交与幂等键复用。列表关联数据改为固定次数批量查询，详情数据库错误不再静默降级为不完整成功响应。已有本地草稿保持只读摘要，避免用原始任务载荷覆盖历史修改。

同日增加默认关闭的唯一真实抖店商品写链：单租户、单白名单已授权店铺、最多 100 SKU 的商品和映射在只读事务中冻结并以 canonical JSON Hash 绑定，经人工审核后才可通过事务 outbox 进入商品刊登 Worker，且只调用 `save_as_platform_draft`。Worker 在平台访问前重复校验任务、审批、执行尝试、下游任务、冻结请求/映射、租户/店铺/商品范围、L3 能力开关、active 灰度和 kill switch；平台成功后的任务、publication、SKU 本地写入事务化。未知平台结果不可自动重试或重建，只能人工只读对账，确认已有草稿后收敛成功。旧直接创建、传统 publish、多目标/批量创建以及刊登任务/批次重试的抖店旁路均在本地写入前固定拒绝。

生产控制面增加数据库级单租户/单店白名单约束、两名不同管理员分别承担 Owner/Technical Lead 审批、revision 冲突保护和 provider/tenant/shop/read/write 五级 kill switch。`L3` 启动只允许平台草稿写，并强制 Provider、网络、凭据、草稿写、P10 Worker、商品刊登队列和任务回收器可用；库存 mutation 与自动业务重试必须关闭。仓库模板仍为 L0，当前未使用真实凭据、未连接真实店铺、未部署或执行真实平台写入，因此状态是“代码候选，等待 CI 与外部人工上线审批”，不是“已上线”。

2026-08-13 将业务任务告警与系统可观测性告警整合到统一告警中心，两者继续使用各自的权限、状态机和 API；告警来源写入 URL，支持刷新与深链恢复。系统告警增加稳定分页、级别/状态/模块筛选、只读展示、确认防重和带原因/有效期的静默处置，确认与静默均写入统一操作日志；可观测性中心只保留运行概览与遥测状态，避免重复处置入口。本次未启用告警扫描 Worker、外部通知渠道或真实平台能力。

2026-08-09 增加全局浅色/深色主题：默认保持浅色，登录后可通过顶部纯图标入口切换，悬浮提示目标主题并在本地持久化选择；2026-08-10 将完整品牌和其右侧的桌面侧栏折叠入口统一移入顶部左侧，侧栏直接从业务导航开始，并增加基于当前账号可见菜单的功能搜索（支持 Ctrl/Cmd + K 与移动端紧凑入口），移动端菜单、品牌、搜索、主题和账户操作保持在同一固定顶栏；共享布局、登录页、运营总览及常用状态表面统一使用 Ant Design 主题 Token，主题切换采用同帧提交并暂停颜色插值，避免顶栏、侧栏和内容出现明暗混色；侧栏抽屉使用不透明主题表面遮挡滚动内容，深色切回浅色会完整恢复浮层与容器样式，登录/注册在移动端采用可滚动的安全居中布局；五档响应式与主题往返回归继续由现有 Admin CI 套件覆盖。

2026-08-11 将 Admin 根路径升级为公开产品官网首页，展示真实的商品采集、AI 优化、人工复核、刊登协同与私有化部署能力；新增独立注册深链，并保持登录、注册 Tab 与 URL 同步。首页复用全局浅色/深色主题并持久化选择，补充滚动显现、主视觉漂浮、卡片悬停和按钮反馈，同时遵循系统减少动态偏好；五档响应式、根节点无横向溢出、主题往返和登录/注册导航继续由现有 Admin CI 套件覆盖。

## 告警机器人通知

2026-08-11 将飞书和企业微信告警通道从开发预留项升级为原生群机器人适配：飞书使用自定义机器人文本消息并支持时间戳签名，企业微信使用群机器人文本消息；两者均校验 HTTP 与厂商业务状态、限制响应摘要、脱敏审计目标并沿用既有 HTTPS、超时、告警去重和总开关边界。生产 Admin 现在展示这两个通道，Webhook 地址与飞书签名 Secret 继续加密存储；未配置或未开启时保持 fail closed。

## 当前边界

- 不在本次整理中提交、推送、打 Tag 或发布 Release。
- 不创建或连接本地测试数据库。
- 不在仓库默认配置中启用真实平台能力；L3 仅由独立发布工单在目标环境启用。
- 后续变更仍须保持核心 CI 绿色，并完成人工验收说明。

## AI 客服生产加固

2026-08-12 修复客服人工外发缺失 `clientMessageId` 导致请求不符合后端幂等契约的问题，并新增默认关闭的店铺级 AI 自动回复。部署级轮询器只为已启用店铺创建去重的增量消息同步任务；新链路在同步后写入幂等运行记录并投递独立 Redis 队列，由租户隔离 Worker 处理。仅最新、尚未人工回复、满足订单上下文和每小时限额的低风险文本可外发，退款赔付等敏感输入与模型承诺输出均转人工。队列、限流、AI 或平台发送失败全部 fail closed、留痕且不自动重试。Admin 提供敏感操作二次确认、只读权限保护和最近运行状态。

该能力不会随代码部署自动启用：Admin 页面中的消息同步、自动回复总开关和店铺策略均默认关闭，保存后动态生效。真实平台上线仍依赖对应 Provider 已验证的消息收取/发送契约、合法店铺授权、AI/Redis 可用性与人工验收；未验证平台继续保持不可用。

同日完成多实例与崩溃恢复加固：Redis 改为 ready/processing 可靠消费并由数据库 pending 扫描补发，自动回复运行使用可续租执行租约；生成阶段可安全恢复，发送阶段结果未知一律转人工。消息同步、人工回复与自动发送共享会话锁并在外发前最终复核；PostgreSQL 新增平台消息、外部会话和单店在途同步唯一约束及热查询复合索引。Admin 切店请求按序隔离，加载失败清空并禁用旧表单，远程店铺搜索采用分页与防抖。

生产验收前进一步收紧发送与运维边界：平台发送幂等租约长于平台超时并持续续期，平台成功且本地消息已落库时，即使幂等完成标记失败也只按 `clientMessageId` 重放本地消息，不再产生可重发窗口。自动回复可用状态改为 Redis、客服消息同步 Worker、轮询调度器和自动回复消费者整链路探活，`/health` 增加 ready/processing 队列与进程状态。`sending` 租约过期会原子转人工并写入统一失败中心；最近运行记录返回截断错误说明。PostgreSQL 去重迁移同时保护 `sent_message_id` 引用。Admin 运行设置加载失败时保持禁用，显式重试成功后才允许保存。
